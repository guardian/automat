# Admin 
GET     /admin/slots                       controllers.SlotsAdminController.index
GET     /admin/slots/:slotId               controllers.SlotsAdminController.show(slotId)
PATCH   /admin/slots/:slotId               controllers.SlotsAdminController.update(slotId)

# Public
POST    /slots                             controllers.SlotsController.create
